/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { FiresrtoreRetroProcessInProgressService } from './firesrtore-retro-process-in-progress.service';

describe('Service: FiresrtoreRetroProcessInProgress', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FiresrtoreRetroProcessInProgressService]
    });
  });

  it('should ...', inject([FiresrtoreRetroProcessInProgressService], (service: FiresrtoreRetroProcessInProgressService) => {
    expect(service).toBeTruthy();
  }));
});
